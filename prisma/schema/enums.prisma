enum UserStatus {
    ACTIVE
    BAN
}

enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum PropertyStatus {
    AVAILABLE
    RENTED
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
