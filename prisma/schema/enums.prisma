enum UserStatus {
    ACTIVE
    BAN
}

enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    UNAVAILABLE
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
