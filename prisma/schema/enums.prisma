enum UserStatus {
    ACTIVE
    BAN
}

enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum PropertyStatus {
    AVAILABLE
    RENTED
    UNAVAILABLE
}

enum RentalRequestStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
